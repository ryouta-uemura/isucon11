# isucon11q 計測メモ（2026-09-19）

このファイルは1日分の計測作業の結論をまとめたもの。手順は `README.md`、
ツールは `tools/` にある。走行データは `runs/<timestamp>-<label>/` に残してある
（git 管理外）。

数字はすべて実測。**推測で書いた箇所には必ずその旨を明記している。**

---

## 0. 結論だけ先に

**1日で 1,315,567 → 4,645,233（約3.5倍）。** 適用した施策は `fix-db-idle-conns` ブランチ。

| 施策 | 効果 | commit |
|---|---|---|
| `db.SetMaxIdleConns` を追加（既定2 → MaxOpenと同値） | **約2倍** | `bfb9998` |
| 確定した日のグラフをメモ化 | **約2倍** | `4d647df` |
| `GET /api/isu/:uuid` を in-memory 化 | +5% | `9f69f6b` |
| `GET /api/isu` を in-memory 化 | **スコア横ばい**（6章参照） | `ac49fed` |
| `postIsuCondition` の `dropProbability 0.9` を削除 | 約4倍（別途、ユーザー既適用） | — |

**測って「やらなくてよい」と分かったもの**:

3台目の追加 / INSERT の queuing / `ssl_session_cache` / `keepalive_requests` /
HTTP/1.1 化 / trend キャッシュの invalidate / `dropProbability` の中間値 /
`level_int` のインデックス追加 / `MaxOpenConns` の増加 / gzip を切る（※4コア構成では未検証）。

**構造的に効くもの**: グラフのスコアは**1時間ブロックあたりのコンディション数**で決まり、
20 を超えると 150点、5以下だと 10点（15倍）。`minTimestampCount` は**全ブロックの最小値**。

**現在地**: サーバ待ちがユーザーループの39%まで下がり、**収穫逓減**に入った。
残り61%はベンチ側の処理時間で触れないため、残る施策をすべてやっても上限は1.63倍。

## 1. 計測環境

> **注**: この章は最初期（アプリVM 2コア + DB同居）の構成。
> 途中で**4コア×2台（app+bench / DB専用）**に組み替えている（4.7 以降）。
> 3章以降の数値は、どの構成で測ったものかを各節で明記している。


```
isucon11q        2コア / 3.8GB / 192.168.252.9    アプリ + MariaDB
isucon11q-bench  4コア / 3.8GB / 192.168.252.10   ベンチ専用
```

**ベンチを別VMに分けたのは必須の措置だった。** 同居構成ではベンチ自身が
2コア中 **0.75コア（41.7%）** を消費しており、アプリの性能を測っているつもりで
ベンチとの奪い合いを測っていた。分離後はスコアが 179k → 240k に上がった。

分離にあたりベンチへ2つパッチを当てている（`tools/bench-patch/`）。特に2つ目は必須:

- `patch-tls.py` — `-tls-skip-verify`。アプリVMの自己署名証明書をベンチVMへ配る代わりに検証を切る。
- `patch-tls-poster.py` — **ポスター(`scenario/posting.go`)は `agent.DefaultTLSConfig` を
  使わず自前の Transport を持つ**ため、1つ目だけでは `POST /api/condition` が
  証明書検証で全滅する。症状が「エラーは出ないのに加点だけゼロ」で分かりにくい。

---

## 2. 作った計測ツール

### レイヤ1: アクセスログの時系列化

nginx を LTSV で吐かせ、**1秒バケット × エンドポイント**に畳んで単一HTMLにする
（`tools/analyze.py`、標準ライブラリのみ + Plotly.js CDN）。
alp/kataribe は全区間の集計しか出さないので「いつ壊れたか」が消える。

log_format の名前は `ltsv` ではなく **`ltsv_ts`** にしてある。競技者側の
`nginx.conf` が既に `log_format ltsv` を定義していることがあり、二重定義は nginx がエラーになるため。

### レイヤ2: スコアの時系列化

ベンチに `-score-dump <path>` を足し、毎秒 JSONL でスコアのスナップショットを出す
（`tools/bench-patch/`）。新しい goroutine は追加せず、既存の3秒ループを1秒に変えて
`sendResult` を3回に1回呼ぶ形にした。**ポータルへの送信頻度は元のまま。**

```json
{"ts":1789746096.91,"raw":12126,"deduction":2,"timeout":27,"score":12124,
 "tags":{"04.GraphWorst":383,"09.ReadInfoCondition":162,...}}
```

`ts` は epoch 秒なので access.log と同じ軸。`POST /initialize` を t=0 として重ねる。
スコア倍率は `bench/scenario/prepare.go` の `Score.Set` と同じ値を持ち、
**最終スナップショットで `sum(count × 倍率) == raw` を検算**して、合わなければ警告を出す。

レポートは8パネル（累積スコア / 秒間加点のタグ別内訳 / 減点 / rps / status / latency /
in-flight / p95ヒートマップ）を x軸共通で縦に並べる。

### 計測オーバーヘッドの検証

`access_log` on/off で nginx は 0.80 vs 0.78 コア（**約2.5%、誤差レベル**）。
ログを出したまま計測して差し支えない。

---

## 3. ベンチの挙動（読解 + 実測）

### ユーザーが増える条件

`verifyTrend` が返す `newConditionNum`（まだ見ていない、かつ実時間2秒以内の condition の数）が
`viewUpdatedTrendCounter` に積まれ、閾値を超えたときだけユーザーが増える。

**trend が古いと `ConditionAlreadyVerified` で弾かれてカウンタが増えず、ユーザーが増えない。
しかも古いデータを返しても減点されない**（`ConditionIsUpdated` は「より古い timestamp」しか弾かない）。
エラーにならずに上限だけが潰れる。

### コンテスタントに開示されている情報

走行中の `score / raw / deduction / timeout` は **3秒ごとに stdout へ出ている**
（`sendResult` のこの2行は `if finish` の外にある）。`ユーザーは増えませんでした` も出る。
終了時にはタグ別カウントも出るが、`_` 始まり（`ViewerDropout`、`PostCondition` など）は
運営専用で stderr にしか出ない。**ContestantLogger は stdout、AdminLogger は stderr。**

つまり素点の推移だけならパッチなしで取れる。自作 `-score-dump` の価値は
**タグ別内訳の時系列**と `_` 系タグ、そして機械可読性に絞られる。

---

## 4. 計測結果

### 4.1 素の実装 → ユーザー実装

| | 素の実装 | main ブランチ |
|---|---|---|
| score | 12,802 | 179,034（同居）/ **240,940**（2VM） |
| `GET /api/trend` p50 / p95 | 1001 / 1010 ms | 1 / 7 ms |
| trend の 499 | 94件 (87%) | 0件 |
| `_4.ViewerLoop` | 6 | 11,943 |
| `_5.ViewerDropout` | 21 | 0 |

素の実装は `GET /api/trend` の 108件中94件がベンチの1秒タイムアウトで切断されていた。
5xx は全エンドポイントで0。**落ちているのではなく間に合っていない**状態。

### 4.2 trend キャッシュの鮮度とスコア

現状の実装は `postIsuCondition` 末尾の

```go
err = tx.Commit()
if err != nil { return ... }           // err != nil はここで抜ける
if err != nil && !latestChanged {      // 到達時点で err は必ず nil → 常に false
    invalidateTrendCache()
}
```

により、**コンディション投稿からの invalidate が一度も発火しない**。

| 戦略 | ユーザー増加 | score（同居） | score（2VM） |
|---|---|---|---|
| 凍結（現状） | 0回 | 172,449 | **240,940 / 242,976** |
| TTL 10s | 1回 | 161,017 | — |
| TTL 1s | 12回 | 63,635 | — |
| TTL 500ms | 11回 | 57,272 | — |
| TTL 200ms | 11回 | 53,846 | — |
| `latestChanged` で invalidate | 9〜11回 | 52,619 | **58,878 / 60,486** |

**鮮度を上げるほど単調に下がる。** ベンチ分離後も結論は変わらず、むしろ差が広がった（3.3倍 → 4.1倍）。

理由: ユーザーが増えると ISU が 48→387 に増え、**0点の `POST /api/condition`** が
76k→141k に倍増し、加点対象の読み取り（GraphWorst 13,903→2,206）が押し出される。
総 rps は上がるのにスコアは下がる。

**ただしこれは「アプリVMが2コアで飽和している」という前提の上での結論。**
容量に余裕があればユーザー増加は純粋な上積みになり、逆転しうる。
凍結はスコアを買っているのではなく飽和を隠している。**未検証**。

### 4.3 アプリVMの CPU 内訳（2VM、gzip on）

| プロセス | コア換算 | 占有率 |
|---|---|---|
| **nginx** | **0.79** | 45.1% |
| isucondition | 0.52 | 29.3% |
| mysqld | 0.44 | 25.0% |
| 合計 | 1.75 / 2 コア | |

**最大の消費者は nginx で、DB ではない。** pprof でもアプリの CPU サンプルの上位は
`runtime.epollwait` / `netpoll` / `findrunnable` という待機ばかりで、
アプリコードは `generateIsuGraphResponse` の 1.55% 程度しか出てこない。

`POST /api/condition` は総 request-time の 57.3% を占めるが **1件 2.19ms と軽い**。
「重い」のは回数であって、1件あたりの処理コストではない。

### 4.4 nginx の CPU の正体は gzip

| | gzip on (level 1) | gzip off |
|---|---|---|
| score | 234,058 | **329,315** |
| nginx | 0.79 コア | **0.56 コア** |
| isucondition | 0.52 | 0.60 |
| mysqld | 0.43 | 0.56 |
| 処理リクエスト | 158,837 | 185,701 |
| 送信量 | 40.5 MB (6 Mbps) | 257.7 MB (36 Mbps) |

gzip が **nginx CPU の 0.23 コア（マシン全体の12%）**を食っている。
切ると解放分がアプリと mysqld に回り、処理量が17%増えてスコアが約40%伸びる。

`gzip_comp_level` は既に 1（最小）なので、調整余地は on/off しかない。
帯域は 6→36 Mbps に増えるが、同一ホスト上のVM間では制約にならない。
`notes.txt` の「転送が20%になった」は事実だが、**この環境では帯域を買って CPU を失う取引**。

中間案として `gzip_min_length` を大幅に上げ、trend のような巨大レスポンスだけ圧縮する手はある。

### 4.5 接続構造と HTTP/2

| 指標 | HTTP/2 | HTTP/1.1 |
|---|---|---|
| nginx CPU | 0.78〜0.79 コア | 0.78〜0.79 コア（**差なし**） |
| TCP 接続数 | 1,144〜1,183 | 4,936〜5,155 |
| 接続あたり平均 | 134〜173 | 31 |
| **同時ストリーム数 最大** | **3**（931接続は最大1） | 2 |
| score (n=7 / n=4) | 224〜246k | 204〜254k |

**多重化はほとんど使われていない。** `http2_max_concurrent_streams`（既定128）に対する
利用率は 2.3%。ベンチのエージェントは1ユーザー=1クライアントで**逐次**にリクエストするため、
実態は HTTP/1.1 の keepalive と同じ「接続の使い回し」。並列性はエージェント数から来ている。

それでも **HTTP/2 を外す利点はなかった**。nginx CPU は同じで、接続数が4.3倍になり、
スコアのばらつきが大きくなる（H2 は幅9%、H1 は幅22%）だけだった。

**エンドポイント単位の出し分けは原理的に不可能。** プロトコルは TLS ハンドシェイク中の
ALPN で決まり、リクエストのパスが分かる前に確定している。クライアント単位でやるなら
listen を別ポート/別IPに分けるしかない。

### 4.6 触る価値がなかったもの

- **`ssl_session_cache`**: TLSハンドシェイクは60秒で **1,081回**のみ（全リクエストの0.68%、
  接続あたり平均147リクエスト）。1回1msと置いても nginx CPU の約3%。
  keepalive が既に効いているのでセッションキャッシュの出番がない。
- **`keepalive_requests` / `http2_max_requests`**: 1000本に到達して打ち切られた接続は
  1,154本中 **136本（11.8%）**のみ。上げても削れるのは1%未満。
- **TLSのバルク暗号**: gzip off で6倍のバイトを暗号化しても nginx CPU は**減る**。
  AES-NI があるのでバルク暗号は実質タダ。「TLSが重い」の重さはハンドシェイク側にある。

なお `upstream` の `keepalive 256`（nginx→アプリ）は別物で、これは効いている。

---

### 4.7 構成を 4コア×2台（app+bench / DB）に変更

ここから先は**ユーザーの 130万点構成を再現した環境**での測定。前節までとは前提が違う。

| VM | スペック | 役割 |
|---|---|---|
| `isucon11q` | 4コア / 4GB | nginx + app（+ ベンチ同居の測定もあり） |
| `isucon11q-db` | 4コア / 4GB | MariaDB 専用 |
| `isucon11q-bench` | 3コア | ベンチ専用 |

**ホストは12コア。VM合計を12に張ると macOS ごと飽和する**（load average 11.62 を実測）。
一度それでベンチVMがCPU不足になり、`GET /api/isu/:uuid/graph` が 7,070→2,883 に半減して
スコアが 392k に落ちた。**構成の優劣ではなく測定の失敗**。

### 4.8 グラフのスコア機構（最も重要）

`bench/scenario/load.go`:
```go
minTimestampCount = 全時間ブロックの min(len(g.ConditionTimestamps))
if minTimestampCount > 20 { return ScoreGraphGood }    // 150点
if minTimestampCount > 10 { return ScoreGraphNormal }  // 100点
if minTimestampCount >  5 { return ScoreGraphBad }     //  60点
return ScoreGraphWorst                                 //  10点
```

**1時間ブロックに 20 件を超えるコンディションが入っていないと Good にならない。
しかも全ブロックの最小値**なので、1つでも薄いブロックがあると全体が引きずられる。

時間スケール（`virtualTimeMulti = 30000`）:
- 1実秒 = 8.3仮想時間
- **グラフの1時間ブロック = 実時間 120ms**
- 1仮想日 = 実時間 2.88秒

投稿レートは `PostIntervalSecond = 60`（仮想60秒に1件）なので、1ブロックの上限は **60件**。
ベンチのソースにも `// MEMO: 最大でも60秒に一件しか送れないので点数上限になる` とある。

### 4.9 dropProbability

`try-current-app-2vm` ブランチには初期実装由来の `dropProbability := 0.9` が残っていた。

| 変種 | score | Good(150) | Worst(10) | mysqld |
|---|---|---|---|---|
| `0.9`（ブランチ現状） | 268,042 / 286,293 | 0 | 21,947 | 1.33 コア |
| `0.0`（rand は呼ぶ） | 1,049,567 / 982,410 | — | — | — |
| ロジックごと削除 | 1,315,567 / 943,326 | 3,834 | 687 | 2.45 コア |

**90%捨てるとブロックが薄くなって全部 Worst(10点)、全部保存すると Good(150点) に化ける。15倍。**

`math/rand` のコストは測定できない（アプリCPU 1.06 vs 1.07 コア、
`POST /api/condition` の p50 9ms で同一）。効いているのは「捨てないこと」だけ。

**中間の drop は成立しない**:

| drop | score | Good | Normal | Bad | Worst | mysqld |
|---|---|---|---|---|---|---|
| 0.0 | 462,567 | **746** | 815 | 82 | 233 | 2.57 コア |
| 0.3 | 330,691 | **0** | 103 | 1,802 | 2,198 | 2.40 コア |
| 0.5 | 221,076 | **0** | 0 | 243 | 7,255 | 2.20 コア |

0.3 で既に Good が全滅する。最小値で決まるうえ、ISU作成直後や進行中の半端なブロックが
必ず混ざるため。**drop 0 でも Good 746 に対し Normal 815 と拮抗しており、素で既にギリギリ。**
DBの節約も 14% しかなく（読み取りクエリが支配的なため）、割に合わない。

### 4.10 律速はCPUではなくレイテンシ

ベンチの通常ユーザーは逐次リクエストなので、ループ1周 = 各レイテンシの合計。

| endpoint | 件数 | 消費秒 | 占有 | 1件 | スコア寄与 |
|---|---|---|---|---|---|
| `GET /api/condition/:uuid` | 40,195 | 185.9 | **57.1%** | 4.62ms | 約237k (25%) |
| `GET /api/isu/:uuid/graph` | 7,070 | 79.1 | **24.3%** | 11.19ms | 約604k (65%) |
| `GET /api/isu` | 5,353 | 23.7 | 7.3% | 4.43ms | **0点** |
| `GET /api/isu/:uuid` | 5,503 | 22.3 | 6.8% | 4.05ms | **0点** |
| 合計 | | **325.7秒** | (462ユーザー秒中70%) | | |

点効率は graph **7,636 点/秒** vs condition **1,275 点/秒**。ただし**ループは固定順序なので
どこを速くしても全周が縮み、全部が増える**。0点の `GET /api/isu` 系（計14%）も同じだけ効く。

**上限の見積もり**: 応答待ち 325.7秒 / クライアント側処理 136秒。
応答を半分にすれば 1.55倍、ゼロにしても **3.4倍が理論限界**。
7ユーザーのままでも150万は射程内、300万超えはユーザー増加なしでは不可能。

### 4.11 SQL の内訳と、接続 churn の発見

slow query log（`long_query_time=0`）、総175.4秒:

| 占有 | 回数 | 平均 | 調査行/回 | クエリ |
|---|---|---|---|---|
| **61.1%** | 75,113 | 1.427ms | - | INSERT INTO isu_condition |
| 17.6% | 75,113 | 0.412ms | 1.0 | UPDATE latest_isu_condition |
| 11.9% | 37,334 | **0.561ms** | 35.6 | SELECT condition |
| 8.1% | 3,803 | **3.743ms** | **648.9** | SELECT graph |

**condition の SELECT は 0.561ms しかない**＝エンドポイント4.62msの12%。
残り約4msはアプリ/ネットワーク側。ここから追って **`SetMaxIdleConns` 未設定**に行き着いた。

```go
db.SetMaxOpenConns(20)   // あった
// SetMaxIdleConns が無い → Go の既定値は 2
```

MaxIdle < MaxOpen だとクエリ完了時にアイドル上限を超えた接続が破棄され、
次のクエリで再接続する。DBが別ホストなので1接続あたり約2ms。

| | 修正前 | 修正後 |
|---|---|---|
| score | 513,715 / 581,668 / 600,183 | **1,187,732 / 1,203,492 / 1,230,016 / 1,497,256** |
| 1走行の新規接続数 | **40,224** | **21** |
| `GET /api/condition` p50 | 4.0ms | **2.0ms** |
| `GET /api/isu/:uuid/graph` p50 | 10ms | **7ms** |

プールサイズ自体（20/50/100）は差が走行間の変動に埋もれる。効くのは
**「MaxIdle >= MaxOpen」という関係だけ**。`MaxOpenConns` は据え置いた。

**この見つけ方は再利用できる**:
1. エンドポイントのレイテンシ内訳を取る
2. slow query log で SQL の実測時間と突き合わせる
3. 差が大きければ pprof（`runtime.futex` / `syscall` が上位なら待ち）
4. `SHOW GLOBAL STATUS LIKE 'Connections'` で接続 churn を確認

**残るSQL改善余地**: graph の SELECT（3.743ms / 648.9行）が単体で最重。
condition 側の `level_int` インデックス追加は無駄（**リクエストの90%が
`condition_level=info,warning,critical` の全レベル指定**で、フィルタにならない）。

### 4.12 3台構成とユーザー増加

ベンチを別VMに出した有効な測定では、**どこも飽和していない**
（app 2.57/4=64%、DB 2.56/4=64%、bench 1.73/3=58%、ホスト load 9.79/12）。
この負荷レベルでは3台目に仕事がない。

ユーザー増加（trendキャッシュの invalidate）は、**飽和していない状態でも負ける**
（285,754 vs 凍結時 541,670）。drop削除後・DB分離後でも同じ（約100万 vs 36万）。

### 4.13 接続 churn（この日で最大の発見）

`GET /api/condition/:uuid` がループの57%を占めていたので slow query log で SQL を測ったところ、
**そのSELECTは 0.561ms しかなかった**（エンドポイントは4.62ms）。SQLは12%で、残りはどこか別。
pprof を見ると `runtime.futex` 24% / `syscall` 20% と、計算ではなく待ちが支配的。
`SHOW GLOBAL STATUS LIKE 'Connections'` が **577,255** で接続 churn を特定した。

```go
db.SetMaxOpenConns(20)   // あった
// SetMaxIdleConns が無い → Go の既定値は 2
```

MaxIdle < MaxOpen だと、クエリ完了時にアイドル上限を超えた接続が破棄され、
次のクエリで再接続する。DB が別ホストなので1接続あたり
TCP handshake + MySQL 認証で約2ms。

| | 修正前 | 修正後 |
|---|---|---|
| 1走行の新規接続数 | **40,224** | **21** |
| `GET /api/condition` p50 | 4.0ms | **2.0ms** |
| `GET /api/isu/:uuid/graph` p50 | 10ms | **7ms** |
| score（ベンチ別VM） | 513,715 / 581,668 / 600,183 | 1,187,732 / 1,203,492 / 1,497,256 |
| score（ベンチ同居） | 943,326 / 1,315,567 | **2,048,251 / 1,985,396** |

プールサイズ自体（20/50/100/150）は差が走行間の変動に埋もれる。
効くのは **「MaxIdle >= MaxOpen」という関係だけ**。

**この見つけ方は再利用できる**:
1. エンドポイントのレイテンシ内訳を取る
2. slow query log で SQL の実測時間と突き合わせる
3. 差が大きければ pprof（`runtime.futex` / `syscall` が上位なら待ち）
4. `SHOW GLOBAL STATUS LIKE 'Connections'` で接続 churn を確認

### 4.14 確定した日のグラフのメモ化

`GET /api/isu/:uuid/graph` 14,388件に対し、ユニークな (ISU, 日) は **1,249件だけ**
（平均11.5回、最大90回の重複要求）。さらに **95.3% が「もう変わらない過去の日」**で、
graph の消費時間の97%、ユーザーループ全体の42%を占めていた。
1仮想日 = 実時間2.88秒と日付が猛烈に進むため、読まれるのはほぼ過去日になる。

**バッチも事前集計テーブルも不要**。要求が来たら作って覚えるだけ（lazy memoization）で足りる。
JSONバイト列で持てばシリアライズも省ける。

**肝**: 「その日が確定したか」の判定に**アプリの壁時計は使えない**。timestamp は仮想時間なので、
**受信した condition の最大 timestamp** を仮想時刻の代理にする。
margin 3600秒は、ポスターが `PostContentNum(10) × PostIntervalSecond(60) = 600仮想秒`
さかのぼった timestamp も送ってくる分。

結果: `GET /api/isu/:uuid/graph` **8.63ms → 0.84ms**（要求数は 14,388 → 32,481 に増加）。
ループのサーバ待ちは 287.7秒 → 197.8秒。score 2,048,251 → 3,920,958。
**ベンチの検証エラーはゼロ**。

### 4.15 収穫逓減に到達

さらに `GET /api/isu/:uuid`（SQL 0.040ms なのに 1.68ms だった。往復が効いていた）と
`GET /api/isu`（isu LEFT JOIN latest_isu_condition）を in-memory 化した。

| | エンドポイント | score |
|---|---|---|
| `GET /api/isu/:uuid` | 1.68ms → **0.66ms** | 約3.9M → 4.12〜4.65M |
| `GET /api/isu` | 1.90ms → **0.70ms** | **横ばい**（3.99〜4.21M） |

**最後の一手はエンドポイントが2.7倍速くなったのにスコアが動かなかった。**
この時点で 462ユーザー秒の内訳が
**サーバ待ち 178秒(39%) / ベンチ側処理 284秒(61%)** になっており、後者は触れない。
16.8秒削っても周回数は約4%増にとどまり、走行間変動（±15%）に埋もれる。

残る最大項 `GET /api/condition/:uuid`（ループの56%）はキャッシュ不可
（47,718件中ユニーク44,592件、平均1.07回。end_time を変えながらページングするため）。
仮に完全にゼロにしても `462/284 = 1.63倍` が理論上限。

### 4.16 【2日目】「収穫逓減」は誤りだった — ループの中と外

4.15 で「上限1.63倍」と書いたが、これは**間違い**だった。2日目に **3,840,000 → 12,400,000（3.2倍）** まで伸びた。

誤りの原因は、時間内訳を **GET だけで数えていた**こと。access.log を method×uri で
分解し直すと、最大項が見えていなかった。

| endpoint | 件数 | 合計 | 占有 |
|---|---|---|---|
| **POST /api/condition/:uuid** | 75,591 | **507.8s** | **73%** |
| GET /api/condition/:uuid | 48,106 | 112.7s | 16% |
| 他すべて | | 84s | 11% |

そして2日目の最大の学びは、**改善が効くかどうかは「ベンチのどのループの中にいるか」で決まる**こと。

ベンチの通常ユーザーは**逐次**にリクエストを出すので、ループ内のレイテンシ短縮は
そのまま周回数になる。一方ポスターと viewer は**タイマー駆動**で、速くしても
出す頻度は変わらない。

| 施策 | 対象 | エンドポイント | score |
|---|---|---|---|
| tx削除 | POST（**ループ外**） | 6.72→5.78ms | **横ばい** |
| プール 20→64 | POST（外） | 5.78→5.45ms | **横ばい** |
| InnoDB 設定 | POST（外） | 5.45→3.95ms | **横ばい** |
| **GET /api/condition 索引化** | **ループ内** | 2.34→0.55ms | **+33%** |

3回続けて「レイテンシは改善したがスコアは動かない」を観測した。**エンドポイントの
ms ではなくループの位置を先に見ること。**

### 4.17 【2日目】DBを起動時の供給源だけにする

`GET /api/condition` は**結果**キャッシュが効かない（ユニーク率93%）が、
**元データ**をメモリに持てばヒット率と無関係に効く。4.15 の「キャッシュ不可」は
結果キャッシュに限った話だった。

- ISU ごとに `[]CondRow` を timestamp 昇順で保持、ロックも ISU 単位で分割
- `end_time`/`start_time` を二分探索 → 後ろ向き走査でレベル一致20件
- 追記は **DB 書き込み成功後**（先に入れると「POSTに成功していない時刻のデータ」エラー）
- 規模は走行終端で 760,655行 / 79 ISU、RSS 130MB。4GBのVMに十分載る

グラフも同じ索引に移すと、`isu_condition` を読むのは `/initialize` の初期618行だけになり、
**INSERT を丸ごと消せる**。`latest_isu_condition` も `getTrend` をメモリ結合にすれば消える。

| | POST | サーバ総時間 | app CPU | score |
|---|---|---|---|---|
| 書き込みあり | 3.95ms | 340〜430s | 0.63コア | 約5,300,000 |
| INSERT 削除 | 2.06ms | 256s | 0.56 | 5,666,878 |
| **+ UPDATE 削除** | **0.63ms** | **131s** | **0.39** | **7,600,000** |

**ここで 4.16 の法則が破れた。** POST はループ外なのに +30% 動いた。
ループの内外は「待ち時間が周回数に直結するか」の話でしかなく、**CPUの奪い合いを
無視していた**。アプリVMは 3.47/4 でほぼ埋まっており、0点の仕事を削ったぶんが
得点経路に回った。**閾値があった。**

使える条件は **アプリが1台であること**。2台以上ならプロセスごとにメモリが分かれ、
片方に書いた condition をもう片方が返せない。

### 4.18 【2日目】gzip を切ると +66%（単独最大）

4.4 の「nginx の CPU の正体は gzip」は正しかった。4コア構成でも成立する。

| 配置 | gzip | score平均 | nginx | 転送量 |
|---|---|---|---|---|
| 同居 | on | 7,382,954 | 1.05コア | 207MB |
| 同居 | **off** | **12,267,715（+66%）** | **0.64コア** | 1,445MB |
| 分離 | on | 3,086,573 | 1.40〜1.60 | 84〜121MB |
| 分離 | **off** | **5,619,480（+82%）** | 1.30〜1.32 | 763〜830MB |

圧縮率は約7:1。60秒で **1.4GB** を圧縮していた。`comp_level 1` でも
毎秒24MBの圧縮は nginx CPU の約4割。

**別VM構成でも勝つ**のが重要。TXオフロードが全滅（`tx-checksum`/TSO が `off [fixed]`）
なので転送量7倍はパケット処理7倍だが、それでも圧縮コストの方が大きい。
本番は実NICでTSOが効くぶん、切る不利はさらに小さい。

**見積もりを外した**: 事前に「nginx CPU の9%程度」と計算したが、
access.log の `size` は**圧縮後**の値。圧縮前は10KB超あり実際は約4割だった。

なお `gzip_static`（事前圧縮を配信、圧縮CPUゼロ）は同居で **-19%**。
ベンチ側の**展開**コストが増え、ベンチが律速だったため。分離では互角（-0.7%）で
減点だけ下がる（17〜20 → 6〜8）。本番想定なら入れてよい。

### 4.19 【2日目】静的ファイルが転送量の3分の2

`/assets/` は `access_log off` で見えていなかった。有効にして測ると:

```
総転送量 7,509 MB / 60秒 = 125 MB/s
うち静的ファイル 4,944 MB（65.8%）
  vendor.ee7444dd.js  726KB × 6,511回 = 4,616 MB
すべて status:200 ── 304 が1件もない
```

ベンチは条件付きリクエストを送らないので `expires 1y` も `immutable` も効かない。

### 4.20 【2日目】score(N) の曲線と最適ユーザー数

ベンチに `BENCH_INIT_USERS` / `BENCH_NO_ADD` を生やしてユーザー数を固定し、
score(N) を直接測った（`tools/bench-patch/patch-fixed-users.py`）。

| N | score | 対N=7 | 周回/人 | 1周あたりGood | ISU |
|---|---|---|---|---|---|
| 7 | 12,162,135 | — | 4,565 | 2.25 | 48 |
| **10** | **13,946,227** | **+14.7%** | 3,847 | 2.14 | 60 |
| 14 | 13,284,712 | +9.2% | 2,832 | 1.93 | 82 |
| 20 | 10,612,462 | -12.7% | 1,770 | 1.62 | 104 |
| 28 | 6,557,044 | -46.1% | 984 | 1.11 | 152 |

**黒字圏は概ね 8〜16人、頂点は N≈10 で +13〜15%。**
3つの独立した測定が一致（同居掃引 +14.7% / 分離掃引 +12.9% / 分離A/B +13.5%）。
分離では **N=28 で減点101 → 失格**。

**減衰の機序**: 1周の時間を分解すると

| N | 1周 | サーバ | ベンチ側 |
|---|---|---|---|
| 7 | 13.04ms | 1.60ms | 11.44ms |
| 28 | 58.89ms | 5.39ms | 53.50ms |

**サーバはどの N でも1割前後。増分の91%はベンチ側。**
当初「ベンチのCPU不足」と考えたが誤り。1人あたり周回数の落ち方は
同居 0.388・分離 0.396（N=7→20）で**ほぼ同一**で、専有4コアでも変わらない。

真因は**ベンチの検証コストがデータ量に比例すること**。`verifyTrend` は trend の
**全ISU**を自前モデルと突き合わせ、ISU は 48→152 に増え、viewer（N に比例、
100msごと）がそれを実行するので**全体で O(N²)**。**アプリからは触れない。
だからアプリを速くしても最適 N は動かない。**

**最大の皮肉**: N=10 は得なのに、到達する手段の方が高くつく。蛇口は trend の
鮮度だけで、TTL10秒（≒N=10相当）は **-4.7%**、直接指定なら **+13.5%**。
差は trend 再構築と viewer 側検証のコスト。**本番では取りに行けない。**

### 4.21 【2日目】プロファイルで見た残り

pprof（`:6060`）で CPU プロファイルを取ると、認証・認可は**ボトルネックではない**。

| | アプリCPU比 |
|---|---|
| `postAuthentication`（JWT ECDSA + セッション発行） | 4.46% |
| `getUserIDFromSession`（全リクエストに乗る） | 1.36% |
| `p256MulInternal`（ECDSA の実体） | 0.58% |
| `queryConds`（テナント判定 + 索引引き） | 0.65% |
| **json.Encode + Decode** | **11.8%** |

セッションはクッキー値をキーにキャッシュ済みで、`securecookie` の検証は
セッションごとに1回しか走らない。全体換算では
`6% × 18%（アプリのシステム内比率）× 11%（ループ内のサーバ比率）< 0.1%`。

プロファイル上位は `runtime.futex` 20.9% / `epollwait` 15.3% / `syscall` 13.8%、
`findrunnable` cum 32.6%。サンプル率 51.29%。**リクエストごとの処理が軽すぎて、
HTTPサーバであること自体のコストが支配的**な状態。

**ユーザーループの内訳は最終的にサーバ11% / ベンチ89%** になり、
サーバを仮にゼロにしても周回数は +12.4% しか増えない。**アプリ側は打ち止め。**

### 4.22 【3日目】nginx を外して Go が直接 TLS 終端

**この構成で nginx を置く理由を数えたら、生きていたのは静的ファイル配信だけだった。**

| 定番の理由 | 我々に当てはまるか |
|---|---|
| 遅いクライアントの吸収（nginx がバッファしワーカーを解放） | **当てはまらない**。1リクエストにプロセス/スレッドが固定される言語では死活問題だが、ゴルーチンで捌く Go では元から不要 |
| TLS/HTTP2 のオフロード | 弱い。Go の crypto/tls で足りる |
| 静的ファイル配信（sendfile） | 当てはまる。ただし Go も `io.Copy` で sendfile を使うので並べる |
| 運用（設定リロード、複数バックエンド分散） | 当てはまらない。60秒・アプリ1台 |

`NO_NGINX=1` で切替。nginx がやっていたこと（TLS/h2、assets のキャッシュヘッダ、
favicon のエイリアス、SPA フォールバック、アイコン配信）をアプリに移した。
`X-Accel-Redirect` は `c.File` に置き換え、**認可ロジックは一切変えていない**。

**A/B（交互3ペア、同居計測）:**

| pair | nginx | nonginx |
|---|---|---|
| 1 | 12,073,421（減点37）| 12,866,810（減点0）|
| 2 | 12,103,328（減点56）| 12,618,240（減点0）|
| 3 | 12,103,523（減点63）| 11,465,086（減点0）|
| 平均 | 12,093,424 | 12,316,712（+1.8%）|

**スコアは互角**（4.21 の通りループの89%がベンチ側なので、アプリのCPUを削っても
周回は増えない）。得たのは別の2つ。

- **減点 37〜63 → 0（3走行すべて）。** 4.18 で gzip を切って以降ずっと
  `FAIL_ERROR_COUNT = 100` の半分を使っていた。中身は走行終了時の
  `/assets/*` への `force closed via ClientConn.Close` で、Go の h2 サーバで消えた。
- **CPU: app+nginx 1.25〜1.29コア → アプリ単独 1.08〜1.11コア（-14%）。**

**分離構成では効果がもっと大きい。** 以前の分離計測（nginx あり）が 5.7〜6.0M に対し、
nginx なしで **7.5〜8.0M（+30%前後）**。分離では nginx の CPU が **1.28〜1.66コア**
（同居 0.64 の2〜2.6倍）まで上がっていたため。理由は `lo` と実インターフェースの差:

| | `lo`（同居） | `enp0s1`（分離） |
|---|---|---|
| MTU | 65536 | 1500 |
| チェックサム | 計算しない | ソフトウェア（`off [fixed]`）|
| セグメンテーション | 不要 | ソフトウェア（TSO/GSO なし）|

転送量 7,509MB は `lo` なら約12万パケット、`enp0s1` なら約520万パケット（**43倍**）。
1つ1つにソフトウェアのチェックサムと分割が乗り、その CPU は書き込みを発行した
プロセス（nginx）に計上される。**本番はベンチが外部機なので分離が現実の形であり、
nginx を外す価値は同居で測った「互角」より大きい。**

**必要な下準備2つ（どちらも原因のわかりにくい形で失敗する）:**

1. `setcap 'cap_net_bind_service=+ep'` で443へのbindを許可。**ビルドし直すと消える。**
2. **`LimitNOFILE` を上げる**（既定のソフト上限1024）。nginx 経由なら
   `X-Accel-Redirect` を返すだけでアプリはファイルを開かないが、外すと静的ファイル
   64,623件/走行を自分で配信し `c.File` が毎回 `os.Open` する。枯渇すると echo の
   `NotFoundHandler` に落ちるので **FD枯渇が 404 として観測される**。
   実際 `GET /api/isu/:uuid/icon` が102件404で失格した。
   **見分け方: エラーが全件「走行最後の1秒」に集中する。**

nginx に戻すときは順序注意。アプリが443を握ったまま `systemctl start nginx` すると
bind に失敗する。先にアプリを unix socket 側へ戻すこと。

### 4.23 【3日目】ベンチをプロファイルした — 周期が伸びる理由

ベンチには最初から `-profile cpu.out` フラグがある（改造不要）。N=7 と N=20 で取って比較。

| | N=7 | N=20 | 差 |
|---|---|---|---|
| 総サンプル | 147.67s（2.25コア）| 179.31s（2.73コア）| +31.6s |
| **`runtime.futex`（待ち）** | 15.02s | 8.94s | **-6.1s** |
| `mallocgc`（cum） | 22.90s | 31.62s | +8.7s |
| `memmove` | 7.89s | 12.96s | +5.1s |
| `crc32`（静的ファイル検証） | 3.69s | 6.78s | +3.1s |
| `scanobject`（GC） | 5.38s | 8.15s | +2.8s |
| `md5`（**prepare フェーズ**） | 9.87s | 8.57s | -1.3s |

**待ちは減り、確保とGCが増えている。CPU も 68〜87% で飽和していない。**
つまり「ベンチのCPU不足」ではない（同居での使用率94%から推測した
2日目の見立ては誤り）。

**注意**: プロファイルは prepare（約5秒）と load（60秒）の両方を含む。
`md5` の6.7%は `verifyIsuIcon` から呼ばれる **prepare フェーズの仕事**で、
ユーザーループとは無関係。**有効なのは N 間の差分だけ。**

`crc32` の増分は `gen/assets.go` の静的ファイル検証。**ベンチは条件付きリクエストを
送らないので `vendor.ee7444dd.js`（726KB）を6,511回落とし毎回CRC32する**（4.19）。

**GC 仮説の検証は失敗した。** `GOGC` を上げる方向はメモリの壁に当たった。

| GOGC | score | 結果 |
|---|---|---|
| 100（既定） | 6,088,907 / 5,769,012 | — |
| 200 | 6,579,188 / 3,041,708 | 判定不能（+8% と -47%）|
| 400 | 0 / 2,552,912 | **OOM**（anon-rss 2.4GB）|
| 1600 | 0 / 0 | **OOM** |

`GOGC=400` で 2.4GB に達し OOM Killer に殺されたことから、
**ベンチのライブヒープは約500MB**（既定でもヒープは1GB前後を往復し、
マーク1回で500MB分を走査している）。

**そして逆の推論が出る。`GOGC` を上げるとベンチCPUは確実に下がった
（3.39→3.25、3.34→2.98）のに、スループットは上がらなかった。**
GC CPU が律速なら減らせば周回が増えるはずで、増えなかった。

**最終的な立場（推測として残す）**: ベンチの**直列な検証処理そのもの**が、
扱うデータ量（ISU 48→104、コンディション履歴）に比例して重くなっている。
GC はその副作用で主因ではない。因果の最終段は**この機材では繋げられなかった**
（ライブヒープ500MBに対しVMが3.9GBでGOGCを十分振れない、
分離のばらつきが±54%）。

いずれの理由でも**アプリからは触れない**。`verify*` がベンチ自身のモデルを
走査する時間は、レスポンスの中身を変えない限り動かず、中身はスキーマで固定。

### 4.24 【3日目】ベンチ実装をどう直すべきか（参考）

自分のコードなら直す順。効く順。

1. **エージェントに静的ファイルのキャッシュを入れる。** サーバは
   `Cache-Control: immutable, max-age=31536000` を返しているのに無視している。
   `If-None-Match` を送れば304で済み、**4.9GBの転送・CRC32・確保が丸ごと消える**。
2. **不変アセットの再検証をやめる。** URL がコンテンツハッシュなので定義上不変。
   一度CRC32したら覚えるだけでよい。
3. **確保を減らす。** ボディバッファの `sync.Pool` 化、リフレクションベースの
   `encoding/json` を生成コード方式に、デコード先構造体の再利用。
4. **正解モデルを有界にする。** 検証に必要な窓だけ持てばライブヒープが一定になり、
   GCスキャンが走行時間に比例して伸びなくなる。
5. **`time.After` を `Ticker`/`Timer.Reset` に。** ユーザーループは1周ごとに
   タイマーを確保している（1ユーザー6,000回/走行 × N人）。
6. **`LastCompletedGraphTime` を意図通り機能させる。** 同じ (ISU, 日) を
   **28.6回**取りに行っている（4.20）。サーバもベンチも無駄。
   ただし直すとスコア機構が変わる。
7. **負荷段階をアプリの挙動から切り離す。** `viewUpdatedTrendCounter` は
   trend の鮮度だけで決まるので、**参加者がキャッシュを凍結すると負荷が上がらない**
   （我々がやったこと）。経過時間とエラー率で決める方が堅牢。

**参加者側への含意**: ベンチの仕事を減らす施策が不釣り合いに効く
（gzip off が nginx の圧縮とベンチの展開の両方を消したので2度効いた）。
逆にベンチに仕事を押し付ける施策は負ける（`gzip_static` が同居で -19%）。

## 5. 実装側で見つかった弱点

1. **`GET /api/isu/:uuid/icon` が誤ったステータスを返す。**（未修正）
   `期待する HTTP ステータスコード以外が返却されました (expected: 401,404,304): 200`。
   最後まで残った唯一の減点源（1走行で7件前後）。`X-Accel-Redirect` 化のときに
   認可と 304 判定が抜けたと思われる。**減点をゼロにできる確実な一手。**
2. **`jiaURL` がグローバル変数で起動時に1度しか読まれない。**（未修正、`main.go` 443行目）
   `postInitialize` が DB を更新してもリフレッシュされないため、JIA の URL を変えたら
   **アプリの再起動が必要**。作業中に2回踏んだ。本番でも URL が変わると事故る。
3. **`bin/run.sh`（2vmブランチ）のバグ。** `cp nginx/*.params` は実ファイル名
   `proxy_params` / `scgi_params` / `uwsgi_params`（アンダースコア）にマッチせず、
   `set -e` で中断する。正しくは `nginx/*_params`。
4. **trend キャッシュの invalidate が死んでいる**（4.2 参照）。
   `tx.Commit()` のエラー時に return 済みなので、後続の `err != nil` が常に false。
   現状は有利に働いているが、意図した動作ではない。
5. **高倍率のスコアタグを取りこぼしている。** `dropProbability` 削除後は
   `01.GraphGood` が主役になったが、`02.GraphNormal`(100点) や `04.GraphWorst`(10点) も残る。
   全部 Good にできれば +10%程度。薄いブロックの特定が要る。

## 6. 途中で誤っていた判断（記録として）

同じ轍を踏まないための記録。この日、**9回外した**。

- **「`ssl_session_cache` と `keepalive_requests` が効く」** → 合わせて nginx CPU の5%未満。
  ハンドシェイク回数を数えずに一般論で挙げたのが誤り。
- **「CPU飽和はアプリの限界」** → 同居構成ではベンチが0.75コアを食っていた。
  システム全体の使用率だけ見てプロセス別に分解していなかった。
- **「slow query log が効いている」** → OFF にしても差は走行間のばらつきの範囲。
- **「多重化していないなら h2 は純損」** → nginx CPU に差は出なかった。
- **6.7秒の停止を SYN 再送と推定** → `$request_time` はハンドシェイクを含まないので的外れ。
- **「ベンチ配置でスコア構成が変わった」** → 違う variant（drop有/無）同士を比較したミス。
- **「アプリVMを2コアに」** → アプリスタックは1.82コア必要で、2コアでは飽和させてしまう提案だった。
- **「drop 0.66 まで平均は20を超えるから中間値でいける」** → 最小値で決まるので 0.3 で全滅。
- **「`GET /api/isu` の in-memory 化で +6%」** → エンドポイントは2.7倍速くなったがスコアは横ばい。

**共通する失敗は「数えずに語ったこと」。** 1走行1.5分で測れるのだから、
仮説は測ってから述べる方が速い。そして**エンドポイントのms改善とスコア改善は一致しない**
（4.15）。改善は必ずスコアで確認すること。

### 6.2 【2日目】さらに外した判断

- **「収穫逓減、上限1.63倍」（4.15）** → 時間内訳を GET だけで数えていた。
  実際は3.2倍伸びた。**内訳は method 込みで分解すること。**
- **「condition はキャッシュ不可」** → *結果*キャッシュの話だった。
  *元データ*をメモリに持てばヒット率と無関係に効く（+33%）。
- **「ループ外だからスコアは動かない」** → 3回正しかったが、書き込みを
  **完全にゼロ**にしたら +30% 動いた。CPUの奪い合いを無視していた。**閾値があった。**
- **「BEGIN/COMMIT は往復2回分だから tx 削除で半減」** → 実際は -14%。
  MariaDB の BEGIN/COMMIT は合計0.94msで、1往復約0.47msしかない。
- **「バッファ枯渇でアプリの write がブロックしている」** → 最大レスポンスは3,252バイトで
  既定の 32KB に収まる。**proxy_buffers は枯渇していない。** 機序が成立しなかった。
- **「gzip は nginx CPU の9%程度だから +40% は再現しない」** → 約4割だった。
  access.log の `size` は**圧縮後**の値。圧縮前のバイト数で計算すべきだった。
- **「ベンチ分離が不利なのはベンチのCPU不足」** → RTTとオフロード全滅が原因。
- **「N増加のダウンサイドの9割はベンチCPU不足」** → 1人あたり周回数の落ち方は
  同居0.388・分離0.396でほぼ同一。専有4コアでも変わらない。真因はベンチの
  検証コストが O(N²) で増えること。
- **「認証・認可は頻度が高いので効く」** → pprof で 0.1% 未満。
  セッションは既にキャッシュ済みで、頻度の高い経路は手当てされていた。

**2日目の教訓**: 1日目の「数えずに語る」に加えて、**「何を数えているか」を間違える**
失敗が多かった（GETだけ、圧縮後サイズ、結果キャッシュ)。
分母と単位を口に出して確認するだけで半分は防げた。

### 6.3 【3日目】さらに外した判断

- **「N増加で周期が伸びるのはベンチのCPU不足」** → プロファイルを取ったら
  `futex` は**減っていた**。アプリVMの使用率94%から推測しただけだった（4.23）。
- **「バックプレッシャーでアプリの write が詰まっている」** → 最大レスポンスは3,252B、
  既定バッファは32KB。**枯渇していない**。機序が成立していなかった。
- **「分離で nginx CPU が上がるのは実ソケットを扱うぶん」** → 説明になっていなかった。
  正しくは `lo` の MTU 65536・チェックサム省略に対し実IFは MTU 1500・
  オフロード全滅で、**同じバイト数が43倍のパケットになる**（4.22）。
- **「パーティショニングで ×3」（ユーザーの案への検討）** → 周期の式
  `max(10ms, サーバ + ベンチ)` のうちサーバは11%。**上限 +7.5%**。
- **「trend を空にすればベンチの確保が減る」** → `verifyTrend` が
  characterSet の完全性と ISU 数を検証するので空にできず、削れても**約1%**。

**3日目の教訓**: 「推測を測って確かめる」までは前日から改善したが、
**プロファイルの対象範囲を確認しなかった**（prepare を含んでいた）、
**検証実験が別の制約に当たった**（GOGC が OOM）という失敗が出た。
実験を設計する時点で「何が先に壊れるか」を考えておくべきだった。

## 7. 計測の罠

- **VMの時刻がずれると `POST /api/auth` が 403 になり全滅する。**
  ベンチの JWT の `iat` が未来に見えて検証に失敗する。負荷でアプリVMが2分遅れた。
  **スコア0の走行を見たらまず3台で `date` を比べる。**
  `sudo systemctl restart systemd-timesyncd` で復旧。
- **ベンチを同居させるとベンチ自身がCPUを食う**（2コア機で0.75コア = 42%）。
- **multipass のVM合計コア数をホストの物理コア数まで張ると、ホストごと飽和する。**
  一度これでベンチが負荷を出し切れず、スコアが構成の違いに見えた（実際は測定の失敗）。
- **走行ごとに access.log をローテートすること。** 混ざると転送量や件数の集計が壊れる。
- **この水準ではスコアの走行間変動が大きい**（同一設定で 943k〜1,316k など、±15〜30%）。
  単発の走行で比較しない。数%の差は解決できない。
- **ビルド失敗を握りつぶさないこと。** `go build ... | head -5 && echo BUILD_OK` は
  失敗しても OK を出す。3走行を無駄にした。

### 7.2 【2日目】追加の罠

- **MariaDB の `slow_query_log` はセッションスコープも持つ。**
  `SET GLOBAL` してもプールの既存接続は古い値を保持し、**アプリのクエリが1件も
  記録されない**（`/initialize` の DDL だけ残る）。有効化後にアプリを再起動すること。
- **`long_query_time=0` の slow log はこの水準では重い。** 2,557,549行を書き出し、
  score が 3.94M → **1.84M** に落ちた。1日目の「差はばらつきの範囲」は93万点時代の話。
- **config の `slow_query_log = 1` が生きていた。** `SET GLOBAL` で切っていても
  **mysqld 再起動で復活**する。config 側を 0 に固定した。
- **MTU 9000 は設定できるが通らない。** ホストのブリッジが1500超を落とし、
  8000バイトで100%ロス。**設定したら必ず戻すこと**（1500超のレスポンスが消える）。
- **`"${arr[@]}"` で展開した `VAR=value` は bash が変数代入と見なさない。**
  コマンド名として扱われて落ちる。`env "${arr[@]}" cmd` とすること。
  掃引スクリプトが1走行も実行せずに終わり、パイプ経由だったため終了コード0で気づかなかった。
- **高負荷走行（N=28など）の後はVMの時計がずれる。** 次の走行で 403 が出る（7章の罠）。
- **分離構成のスコアばらつきは ±54%** と同居（±6.6%）より桁違いに大きい。
  分離での判定は score より**周回数**を見る方が安定する。
- **静的ファイルは `access_log off` で見えない。** 転送量の3分の2がそこにあった（4.19）。

### 7.3 【3日目】追加の罠

- **FD枯渇が 404 として現れる。** nginx を外すとアプリが `c.File` で毎回 `os.Open` し、
  ソフト上限1024で枯渇する。`os.Open` の失敗は echo の `NotFoundHandler` に落ちるので
  **404 になる**。エラーメッセージが原因を全く示さない。
  **見分け方: エラーが全件「走行最後の1秒」に集中する。**
- **nginx とアプリで 443 を取り合う。** A/B スクリプトには
  **443の所有者チェックを必ず入れること**（`ss -tlnp | grep :443`）。
  これが無ければ「nginx ありのはずが実はアプリが応答していた」6走行を
  正しいデータとして受け取っていた。
- **プロファイルの対象範囲を確認すること。** ベンチの `-profile` は
  プロセス起動から取るので prepare フェーズを含む。`md5` の6.7%は prepare の仕事で、
  ユーザーループとは無関係だった。**N 間の差分だけが有効。**
- **`GOGC` を上げるとベンチが OOM で死ぬ。** ライブヒープ約500MBに対し
  `GOGC=400` で anon-rss 2.4GB。`dmesg | grep -i oom` で確認できる。
- **`nginx` を外すと access.log が消えるので、内訳の分解ができなくなる。**
  計測用と本番用で設定を分ける運用が必要。

## 8. 現在の環境の状態

| VM | スペック | 役割 |
|---|---|---|
| `isucon11q` | 4コア / 192.168.252.9 | nginx + app（+ ベンチ同居） |
| `isucon11q-db` | 4コア / 192.168.252.11 | **実質遊休**（DB書き込み全廃。mysqld 1.4 CPU秒/走行）。分離計測時のベンチ置き場 |

**score: 約12,300,000（同居計測）/ 約7,500,000（分離計測）。減点 0。**

`NO_NGINX=1` で nginx は停止中（アプリが443を直接持つ）。
`setcap` と `LimitNOFILE` の下準備が必要（4.22）。

| | 1日目の朝 | 2日目の終わり |
|---|---|---|
| `GET /api/condition` | 2.34ms | 0.36ms |
| `GET /api/isu/:u/graph` | 0.88ms | 0.34ms |
| `POST /api/condition` | 6.72ms | 0.16ms |
| サーバ総時間 | 704.9s | 65.4s |
| アプリ + nginx CPU | 2.12コア | **1.10コア**（nginx 撤去）|
| 減点 | 11〜43 | **0** |

環境変数（`/home/isucon/env.sh`）で切り替えられる:

- `COND_CACHE=1` — condition/graph をインメモリ索引から返す（0 で DB 経路に戻る）
- `COND_INSERT=0` — `isu_condition` / `latest_isu_condition` への書き込みを止める
- `TREND_CACHE_TTL_MS=0` — trend キャッシュの寿命。0 は凍結
- `NO_NGINX=1` — nginx を外し Go が直接443でTLS終端

nginx は `gzip off`、`gzip_static` なし、assets は `access_log off`。
事前圧縮した `.gz` は10ファイル置いたまま（ディレクティブが無いので無害）。

ベンチ側（`BENCH_ENV` で渡す。`tools/bench-patch/patch-fixed-users.py` 適用済み）:

- `BENCH_INIT_USERS=N` — 総ユーザー数を固定
- `BENCH_NO_ADD=1` — ユーザー増加を止める

計測ツール: `tools/measure.sh`（`BENCH_ENV` 対応、コア数は `nproc` から取得）、
`tools/sweep-trend-ttl.sh`、`tools/sweep-gzip.sh`、`tools/sweep-users.sh`（`PLACEMENT=split` 可）。

バックアップ: `main.go.{beforetxdrop,beforepool64,beforettl,beforecondcache,beforegraphmem,beforenoinsert}`、
`scenario/load.go.orig`（ベンチ）、`50-server.cnf.before-innodb-tune`、
`isucondition.conf.before-gzipstatic`、`/etc/hosts.before-bench`（DB VM）。

コミット: ブランチ `fix-db-idle-conns`、`df41160`（1日目終わり）〜`367be20`。

## 9. どこまで来て、何が残っているか

**アプリ側は打ち止め。** ユーザーループの内訳がサーバ11% / ベンチ89%（4.21）で、
サーバを仮にゼロにしても周回数は +12.4% しか増えない。pprof 上位は
スケジューラと syscall で、認証・JSON もスコア換算では 0.2% 未満。

**構造的に閉じているもの:**

1. **ユーザー増加** — N≈10 が最適で +13〜15% だが、到達する手段（trend の新鮮化）の
   方が高くつく（4.20）。減衰の主因はベンチ側 O(N²) でアプリからは触れない。
2. **`04.GraphWorst` 8,288件** — 各イテレーションの最後に到達する「ISU作成日」が
   必ず部分日になるため。存在しないデータは作れない。
3. **静的ファイルの再送** — ベンチが条件付きリクエストを送らないので 304 にできない（4.19）。

4. **周期の床** — ベンチのユーザーループには `time.After(10ms)` のペーシングがある。
   1周 12.93ms（サーバ 1.40 / ベンチ 11.53）に対し床が 10ms なので、
   **サーバを完全にゼロにしても +12.1%、床に張り付いても +29.3%** が上限（4.21）。
5. **ベンチ内部の検証コスト** — データ量に比例して重くなる。プロファイルで
   確保・memmove・GC の増加として観測できるが、**アプリからは触れない**（4.23）。

**未検証で残っているもの:**

1. **決定的な間引き** — 1仮想時間60件に対し21件で足りる。ただし
   `09.ReadInfoCondition`（268,400点）の目減りと相殺で、要測定。
   **ランダムは不可**（最小値で決まるため。4.8）。
2. **`gzip_static`** — 分離では互角だが減点が下がる。ただし nginx 撤去で
   減点は既に0なので、意味は薄れた。
3. **周期の因果の最終段** — 「確保とGCが増える」と「周期が伸びる」の間。
   この機材では繋げられなかった（4.23）。メモリを増やしたVMなら `GOGC` を
   十分振れる可能性がある。

**やらない方がよいと判断したもの:**

- **ユーザー単位のパーティショニング（3台構成）** — 周期の式
  `max(10ms, サーバ + ベンチ)` のうちサーバは11%。3台に割っても **上限 +7.5%**。
  そのために `/api/trend` の全ISU集約と uuid→担当サーバの表という分散状態を
  2つ抱えることになる。そもそもアプリは4コア中1.10コアしか使っていない。
- **trend レスポンスの最小化** — `verifyTrend` が characterSet の完全性と
  ISU 数を検証するので空にできず、削れても約1%。失敗すると viewer が即死する。
