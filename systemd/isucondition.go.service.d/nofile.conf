[Service]
# NO_NGINX=1 でアプリが静的ファイルを自分で配信するとき、c.File が
# リクエストごとに os.Open する。既定のソフト上限1024では枯渇し、
# os.Open の失敗が echo の NotFoundHandler 経由で 404 として観測される
# （GET /api/isu/:uuid/icon が 102件 404 になり失格した）。
# 全件が走行最後の1秒に集中するのが見分け方。
LimitNOFILE=1048576
